name: close-position

on:
  workflow_dispatch: {}   # triggered from the dashboard's "Close position" button

permissions:
  contents: write

concurrency:
  group: trade            # never run at the same time as a trading cycle
  cancel-in-progress: false

jobs:
  close:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install requests
      - name: Close open position
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python main.py close BTC_USD
      - name: Commit portfolio state
        run: |
          git config user.name "paper-trade-bot"
          git config user.email "bot@users.noreply.github.com"
          git add portfolio.json
          git diff --cached --quiet || git commit -m "manual close $(date -u +%FT%TZ)"
          git pull --rebase
          git push
